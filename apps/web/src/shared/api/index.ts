/**
 * Single entry point for all web API interactions.
 *
 * Usage:
 *   import { API_ROUTES, apiFetch, sendOtp, verifyOtp } from "@/shared/api";
 *   // or
 *   import { API_ROUTES } from "./routes";
 */

export * from "./routes";
export * from "./client";
export * from "./auth";
